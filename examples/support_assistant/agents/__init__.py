# agents/ — autodiscover() imports every module here at startup, registering
# each Agent at class-definition time. Drop a new agent file in and it goes
# live on the next start; no list to maintain.
